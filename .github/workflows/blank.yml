name: Hello World
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: build phase
        run: echo "Build Phase done"
  test:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: test phase
        run: echo "Test Phase Done"
  deploy:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Deploy Phase
        run: echo "Deploy Phase Done"
        
        
                  
        
  
